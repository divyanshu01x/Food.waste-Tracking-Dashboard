# Food.waste-Tracking-Dashboard
This webpage is a real-time IoT dashboard for ESP32 load cell systems using HX711. It shows live weight, device status, ADC data, charts, and logs across multiple nodes. Currently simulated, it can connect to real hardware via HTTP, WebSocket, or MQTT for applications like smart weighing and monitoring.
